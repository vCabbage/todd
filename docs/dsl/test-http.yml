---
# Example test file
type: testrun
label: test-http
spec:
    targettype: group
    source:
        name: headquarters
        app: curl
        args: "-c"
    target:
        name: datacenter
        app: nginx
        args: "-s"
